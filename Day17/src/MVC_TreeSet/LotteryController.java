package MVC_TreeSet;

import java.util.HashSet;

import Set.Hash;

public class LotteryController {
	HashSet<Lottery> lottery =new HashSet<>();
	HashSet<Win> win = new HashSet<>();
	
	boolean insertObject(Lottery l) {
		set 
		add
	}
	boolean deleteObject(Lottery l) {
		remove
		clear
		
	}
	HashSet winObject() {
		all
		System.out.println();
	}
	TreeSet sortedWinObject() {
		System.out.println();
	}
	boolean searchWinner(Lottery l) {

		for(String l : win.keySet(String name, String phone)) {
			if(l.equals(win) && win.get(l).getPass().equals(pass)) {
				
				for(String res2: win.keySet()) {
					System.out.println(win.get(res2));			
				}
				
				return res +"축하합니다. 당첨목록에 존재합니다.";
			}
		}		
		
		return "안타깝 당첨목록에 존재하지 않음";
	}	}
}

function send(){
    let title = document.querySelector('title');
    let content = document.querySelector('content');
    let newtd = document.querySelector('')

    if(title && content){

    }
}